/**
 * @yusufolosun/stacks-next/react
 *
 * React hooks and components for Stacks dApp development.
 *
 * NOTE: When using this module in Next.js App Router,
 * add 'use client' directive at the top of your component file.
 *
 * @example
 * ```typescript
 * 'use client';
 *
 * import {
 *   StacksProvider,
 *   useConnect,
 *   useStacksAccount,
 *   useContractRead,
 *   useContractWrite,
 * } from '@yusufolosun/stacks-next/react';
 * ```
 *
 * @packageDocumentation
 */

// React hooks and components will be implemented in Phase 4
// For now, export a placeholder to ensure the build works

/**
 * Placeholder - React integration coming in Phase 4
 *
 * This module will include:
 * - StacksProvider - React context provider
 * - useStacksConfig - Access config from context
 * - useConnect - Wallet connection hook
 * - useStacksAccount - Account info hook
 * - useContractRead - Read-only contract call hook
 * - useContractWrite - Transaction hook
 * - useTransaction - Transaction status hook
 * - useBnsName - BNS resolution hook
 * - useSbtcBalance - sBTC balance hook
 */

export const REACT_MODULE_VERSION = '0.1.0';
